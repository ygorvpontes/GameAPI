# Configura��es do Banco de Dados MySQL (Produ��o)
spring.datasource.url=jdbc:mysql://localhost:3306/gameapi_db?createDatabaseIfNotExist=true
spring.datasource.username=seu_usuario_mysql
spring.datasource.password=sua_senha_mysql
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Hibernate DDL (Data Definition Language) para produ��o
# 'validate' verifica se o schema do banco bate com as entidades, mas n�o altera nada.
# � mais seguro para n�o perder dados em produ��o.
spring.jpa.hibernate.ddl-auto=validate

# Desativa o console do H2 em produ��o por seguran�a
spring.h2.console.enabled=false